// datatype - 8

// primitive data type-> for single value storing
    // string
    // number
    // boolean
    // undefined
    // null
    // bigint
    // symbol

// non-primitive data type -> for multiple value storing
    //object

const courseName ="Js";
const price = 1000;
const isAvailable = false;

// undefined
let rating;
console.log(rating);
// only work with let and var

// undefined -> when nothing is stored in a variable
// it is a data type

let rating1 = null;
console.log(rating1);

// null is stored inside rating1 as a value

//object
 const course = {
    name: 'Js',
    price: 1000,
    isAvailable: false,
    let: 123,
    234: 10, // stored in backend as string
 } 

console.log(course);

// object property: value or key: value

// object -> collection of data but unordered

// array is a variation of object 
// array is ordered
const languages = ["Malayalam", "Tamil", 123] // any data type can be given inside array in javascript

console.log(languages[0]);

const one = 1
console.log(one);

const two = "two"
console.log(two);




